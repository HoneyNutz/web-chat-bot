[project]
name = "personal-website-embed"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "sentence-transformers>=3.0.0",
  "numpy>=1.26.0",
]

[tool.uv]
index-url = "https://pypi.org/simple"
